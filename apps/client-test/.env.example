# Solana private key — base58-encoded secret key or path to keypair JSON
SOLANA_PRIVATE_KEY=
# EVM private key — hex-encoded (optional, for EVM endpoints)
EVM_PRIVATE_KEY=
# Target endpoint to test against
TARGET_URL=https://helius.api.corbits.dev
