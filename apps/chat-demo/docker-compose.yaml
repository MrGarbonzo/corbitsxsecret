services:
  chat-demo:
    image: ghcr.io/mrgarbonzo/corbitsxsecret-test-chat-demo:latest
    ports:
      - "80:3000"
    environment:
      - PORT=3000
      - TARGET_URL=${TARGET_URL:-https://my-test-proxy.garbonzo.api.corbits.dev}
    restart: unless-stopped
